import toolsConst from './tools-const'

// 侧边栏选项
// eslint-disable-next-line
export const toolsConstMap = new Map([
    [toolsConst.TOOLS_OPTIONS.INNERARCHIVE, 's1'],
    [toolsConst.TOOLS_OPTIONS.FRIENDS, 's2'],
    [toolsConst.TOOLS_OPTIONS.ABOUTME, 's3'],
    [toolsConst.TOOLS_OPTIONS.CLOSE, 's4']
])
