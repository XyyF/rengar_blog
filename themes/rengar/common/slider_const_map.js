import sliderConst from './slider_const'

// 侧边栏选项
// eslint-disable-next-line
export const sliderConstMap = new Map([
    [sliderConst.SLIDER_OPTIONS.INNERARCHIVE, 's1'],
    [sliderConst.SLIDER_OPTIONS.FRIENDS, 's2'],
    [sliderConst.SLIDER_OPTIONS.ABOUTME, 's3'],
    [sliderConst.SLIDER_OPTIONS.CLOSE, 's4']
])
